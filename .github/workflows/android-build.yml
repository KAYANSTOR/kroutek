name: Android CI

on:
  push:
    branches: [ "main", "session/**" ]
    paths:
      - 'kurotek/**'
      - 'gradle/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'gradle/libs.versions.toml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'kurotek/**'
      - 'gradle/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'gradle.properties'
      - 'gradle/libs.versions.toml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JAVA_VERSION: '17'
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      BUILD_REPORTS_DIR: ${{ github.workspace }}/kurotek/build/reports

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          sdk-level: 35
          build-tools-level: 35.0.0

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Grant execute permission to gradlew
        run: chmod +x gradlew

      - name: Decode secrets from env if present
        env:
          ENCODED_ENV: ${{ secrets.ENCODED_ENV }}
        run: |
          if [ -n "$ENCODED_ENV" ]; then
            echo "$ENCODED_ENV" | base64 -d > .env
          fi

      - name: Run unit tests
        run: ./gradlew :kurotek:testDebugUnitTest --stacktrace --no-daemon

      - name: Run lint
        run: ./gradlew :kurotek:lintDebug --stacktrace --no-daemon

      - name: Build debug APK
        run: ./gradlew :kurotek:assembleDebug --stacktrace --no-daemon

      - name: Build release APK if secrets are available
        if: ${{ secrets.KEYSTORE_BASE64 != '' && secrets.KEY_ALIAS != '' && secrets.STORE_PASSWORD != '' && secrets.KEY_PASSWORD != '' }}
        run: |
          mkdir -p keystore
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > keystore/upload.jks
          echo "KEYSTORE_PATH=${{ github.workspace }}/keystore/upload.jks" >> $GITHUB_ENV
          echo "KEY_ALIAS=${{ secrets.KEY_ALIAS }}" >> $GITHUB_ENV
          echo "STORE_PASSWORD=${{ secrets.STORE_PASSWORD }}" >> $GITHUB_ENV
          echo "KEY_PASSWORD=${{ secrets.KEY_PASSWORD }}" >> $GITHUB_ENV
          ./gradlew :kurotek:assembleRelease --stacktrace --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: kurotek-debug-apk
          path: kurotek/build/outputs/apk/debug/*.apk
          retention-days: 30

      - name: Upload release APK if built
        if: ${{ secrets.KEYSTORE_BASE64 != '' && secrets.KEY_ALIAS != '' && secrets.STORE_PASSWORD != '' && secrets.KEY_PASSWORD != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: kurotek-release-apk
          path: kurotek/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: Upload build reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kurotek-build-reports
          path: |
            ${{ env.BUILD_REPORTS_DIR }}/**/*
          retention-days: 30

      - name: Upload lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kurotek-lint-reports
          path: |
            kurotek/build/reports/lint-results*.html
            kurotek/build/reports/lint-results*.xml
          retention-days: 30

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kurotek-test-reports
          path: |
            kurotek/build/reports/tests/**/*
          retention-days: 30

      - name: Upload mapping file if release built
        if: ${{ secrets.KEYSTORE_BASE64 != '' && secrets.KEY_ALIAS != '' && secrets.STORE_PASSWORD != '' && secrets.KEY_PASSWORD != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: kurotek-release-mapping
          path: |
            kurotek/build/outputs/mapping/release/mapping.txt
          retention-days: 30
